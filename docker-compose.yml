version: "3.9"

services:
  postgres:
    image: postgres:alpine3.20
    container_name: coupon-project-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: "MAK#9555"
